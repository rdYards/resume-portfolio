{
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && echo \"/* /index.html 200\" > dist/_redirects",
    "preview": "vite preview"
  },
  "dependencies": {
    "preact": "^10.25.3",
    "preact-iso": "^2.11.0"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.9.3",
    "vite": "^6.0.4"
  }
}
